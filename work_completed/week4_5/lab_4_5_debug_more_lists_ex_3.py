# The bug in the code was that it was calculating sum incorrectly.
# Readjusted code for proper vocabulary and inserted numbers in sum() function.
numbers = list(range(10,21))
sum_of_numbers = sum(numbers)
print(sum_of_numbers)
